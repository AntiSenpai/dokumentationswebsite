{# base.html.twig #}

<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}NNC{% endblock %}</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
    <link rel="stylesheet" href="{{ asset('css/navbar.css') }}">
</head>
<body>
    <nav class="navbar-nav-top">
        <div class="navbar-nav-left">
            <img src="{{ asset('images/detaillogo.png') }}" alt="NNC Logo">
        </div>
        <div class="navbar-nav-right">
        <div class="user-menu">
            <div class="user-info-box">
                <span>Wilkommen, {{ app.user.username }}</span> |
                 {% if is_granted('ROLE_ADMIN') %}
                | <a href="{{ path('admin_area') }}">Admin-Bereich |</a>
            {% endif %} 
                <a href="{{ path('app_logout') }}">Abmelden</a>
            </div>
        </div>
        </div>
    </nav>

    <nav class="navbar-nav-main">
        <ul class="nav-nav">
            <li><a href="{{ path('home') }}" class="nav-nav-link">Übersicht</a></li>
            <li><a href="{{ path('dokumentation') }}" class="nav-nav-link">Kundendoku</a></li>
            <li><a href="{{ path('zeiterfassung') }}" class="nav-nav-link">Zeitraffer</a></li>
        </ul>
    </nav>

    {% block body %}
    {% endblock %}

<footer class="text-center text-muted py-3">
    <p>&copy; 2023 NNC IT</p>
</footer>

</body>
</html>
